<Window
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Quicker.Windows.Menus"
    x:Class="Quicker.Windows.Menus.DownloadWindow"
    mc:Ignorable="d"
    
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    WindowStyle="None"
    Topmost="True"
    Height="93.8"
    Width="300"
    
    MouseEnter="DownloadWindow_MouseEnter"
    MouseLeave="DownloadWindow_MouseLeave"
    Loaded="DownloadWindow_Loaded"
>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="53*"/>
            <ColumnDefinition Width="247*"/>
        </Grid.ColumnDefinitions>
        <Grid Background="#F9F9F9" Height="28" VerticalAlignment="Top" Grid.ColumnSpan="2">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Text="Quicker" Foreground="#909090" Margin="5,0" FontSize="11" FontWeight="Bold"/>
            <Button x:Name="ClearButton" Margin="270,4,10,0" ToolTip="清理" Style="{StaticResource DownloadWindowButton}" Click="ClearButton_Click">
                <Image Source="/Resources/Images/Icons/Quicker1.ico" Width="20"/>
            </Button>
        </Grid>
        <TextBlock x:Name="DownloadFileName" FontWeight="Bold" Grid.ColumnSpan="2" Margin="10,30,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="255" Height="19" FontSize="11"/>
        <Button x:Name="CancelButton" Margin="0,30,10,0" ToolTip="取消" Style="{StaticResource DownloadWindowButton}" Grid.Column="1" Click="CancelButton_Click">
            <Image Source="/Resources/Images/Icons/Quicker1.ico" Width="20"/>
        </Button>
        <ProgressBar x:Name="DownloadProgressBar" Margin="10,54,10,33" Grid.ColumnSpan="2"/>
        <TextBlock x:Name="DownloadSpeed" Text="0 KB/S" Margin="10,66,0,0" FontSize="11" HorizontalAlignment="Left" Foreground="Gray" VerticalAlignment="Top"/>
        <TextBlock x:Name="DownloadSize" Margin="0,65,146,11" Grid.Column="1" FontSize="11"/>
    </Grid>
</Window>