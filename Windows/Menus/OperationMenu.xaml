<Window 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Quicker.Windows"
    x:Class="Quicker.Windows.OperationMenu"
    mc:Ignorable="d"

    AllowsTransparency="True"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    WindowStyle="None"
    Background="{x:Null}"
    Topmost="True"
    Height="451"
    Width="784"
    
    Deactivated="OperationMenu_Deactivated"
>
    <Grid x:Name="MainGrid" Height="117" Width="204">
        <!-- 窗口阴影 -->
        <Rectangle Fill="White" Margin="5,7">
            <Rectangle.Effect>
                <DropShadowEffect Color="#FFBBBBBB" Direction="0" BlurRadius="30" RenderingBias="Quality" ShadowDepth="1"/>
            </Rectangle.Effect>
        </Rectangle>
        <Border CornerRadius="5" Background="White">
            <StackPanel x:Name="MainStackPanel">
                <Button x:Name="EditeInformation" ToolTip="编辑动作定义" Style="{StaticResource MenuButton}" Margin="0,5,0,3" Click="EditeInformation_Click">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/EditButton.ico" Style="{StaticResource MenuButtonImage}"/>
                        <TextBlock Style="{StaticResource MenuButtonTextBlock}" Text="编辑"/>
                    </Grid>
                </Button>
                <Rectangle Height="1" Fill="#FFF0F0F0" Width="204"/>
                <Button x:Name="DeleteAction" ToolTip="删除动作" Style="{StaticResource MenuButton}" Margin="0,3,0,0" Click="DeleteAction_Click">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/DeleteImage.ico" Style="{StaticResource MenuButtonImage}"/>
                        <TextBlock Style="{StaticResource MenuButtonTextBlock}" Text="删除"/>
                    </Grid>
                </Button>
                <Button x:Name="CheckImformation" ToolTip="查看动作信息" Style="{StaticResource MenuButton}" Click="CheckImformation_Click">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/ActionInformation.ico" Style="{StaticResource MenuButtonImage}"/>
                        <TextBlock Style="{StaticResource MenuButtonTextBlock}" Text="查看信息"/>
                    </Grid>
                </Button>
                <Button x:Name="OpenLocation" ToolTip="在资源管理器中找到对应文件或文件夹" Style="{StaticResource MenuButton}" Margin="0,0,0,5" Click="OpenLocation_Click">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/OpenFile.ico" Style="{StaticResource MenuButtonImage}"/>
                        <TextBlock Style="{StaticResource MenuButtonTextBlock}" Text="打开所在位置"/>
                    </Grid>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>