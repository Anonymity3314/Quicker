<Window
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    xmlns:local="clr-namespace:Quicker.Windows"
    x:Class="Quicker.Windows.CustomMenu"
    mc:Ignorable="d"
    
    WindowStartupLocation="Manual"
    AllowsTransparency="True"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    Background="{x:Null}"
    WindowStyle="None"
    Topmost="True"
    Height="190"
    Width="220"
    
    Deactivated="CustomMenu_Deactivated"
>
    <Window.Resources>
        <Style x:Key="Button" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="White" BorderBrush="White" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFEAEAEA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TextBlock" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="30,0,0,0"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Width" Value="166"/>
        </Style>
        <Style x:Key="Image" TargetType="Image">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="7,0,0,0"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Width" Value="166"/>
        </Style>
    </Window.Resources>
    
    <Grid Height="159" Width="200">
        <!-- 窗口阴影 -->
        <Rectangle Fill="White" Margin="5,7">
            <Rectangle.Effect>
                <DropShadowEffect Color="#FFBBBBBB" Direction="0" BlurRadius="30" RenderingBias="Quality" ShadowDepth="1"/>
            </Rectangle.Effect>
        </Rectangle>
        <Border BorderBrush="White" BorderThickness="0" CornerRadius="5" Padding="1" Background="White">
            <StackPanel Margin="-1,-1,-1,-1">
                <Button Click="ShowMainWindow" ToolTip="弹出面板窗口" Style="{StaticResource Button}" Margin="0,5,0,0">
                    <Grid>
                        <Image x:Name="showMainWindow" Source="/Resources/Images/Icons/OpenMainWindow1.ico" Style="{StaticResource Image}" Margin="7,0,0,0" Width="16"/>
                        <TextBlock Text="显示面板窗口" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="OpenSettingWindow" ToolTip="打开设置窗口" Style="{StaticResource Button}">
                    <Grid>
                        <Image x:Name="openSettingWindow" Source="/Resources/Images/Icons/SettingImage1.ico" Style="{StaticResource Image}" Margin="7,0,0,0" Width="16"/>
                        <TextBlock Text="设置" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="OpenActionPageManageWindow" ToolTip="打开场景与动作管理窗口" Style="{StaticResource Button}">
                    <Grid>
                        <Image x:Name="openActionPageManageWindow" Source="/Resources/Images/Icons/ActionPagesManager.ico" Style="{StaticResource Image}"/>
                        <TextBlock Text="场景与动作" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="PauseQuicker" ToolTip="暂停和恢复Quicker触发" Style="{StaticResource Button}">
                    <Grid>
                        <Image x:Name="pauseQuicker" Source="/Resources/Images/Icons/Pause.ico" HorizontalAlignment="Left" VerticalAlignment="Center" Height="16" Width="16" Margin="7,0,7,0"/>
                        <TextBlock Text="暂停" x:Name="PauseQuickerTextBlock" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <!--<Button Click="Button_Click" Style="{StaticResource Button}">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/Quicker1.ico" HorizontalAlignment="Left" VerticalAlignment="Center" Height="16" Width="16" Margin="7,0,7,0"/>
                        <TextBlock Text="开启文本悬浮窗" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="Button_Click" ToolTip="录制键盘和鼠标操作并生成动作" Style="{StaticResource Button}">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/Quicker1.ico" HorizontalAlignment="Left" VerticalAlignment="Center" Height="16" Width="16" Margin="7,0,7,0"/>
                        <TextBlock Text="键盘录制工具" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="Button_Click" Style="{StaticResource Button}">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/Quicker1.ico" HorizontalAlignment="Left" VerticalAlignment="Center" Height="16" Width="16" Margin="7,0,7,0"/>
                        <TextBlock Text="窗口检查工具" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="Button_Click" Style="{StaticResource Button}">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/Quicker1.ico" Style="{StaticResource Image}"/>
                        <TextBlock Text="重新加载键鼠挂钩" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="Button_Click" Style="{StaticResource Button}">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/Quicker1.ico" Style="{StaticResource Image}"/>
                        <TextBlock Text="重置键盘状态" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="Button_Click" Style="{StaticResource Button}">
                    <Grid>
                        <Image Source="/Resources/Images/Icons/Quicker1.ico" Style="{StaticResource Image}"/>
                        <TextBlock Text="终止动作" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>-->
                <Button Click="Restart" ToolTip="退出并重新启动" Style="{StaticResource Button}">
                    <Grid>
                        <Image x:Name="restart" Source="/Resources/Images/Icons/RestartQuicker.ico" Style="{StaticResource Image}" Margin="7,0,0,0" Width="16"/>
                        <TextBlock Text="重启Quicker" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
                <Button Click="Exit" ToolTip="关闭Quicker软件" Style="{StaticResource Button}">
                    <Grid>
                        <Image x:Name="exit" Source="/Resources/Images/Icons/CloseQuicker.ico" HorizontalAlignment="Left" VerticalAlignment="Center" Height="16" Width="16" Margin="7,0,7,0"/>
                        <TextBlock Text="退出" Style="{StaticResource TextBlock}"/>
                    </Grid>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>