<ResourceDictionary
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
>
    <!-- 自定义 Thumb 样式，用于滚动条的滑块 -->
    <Style x:Key="ScrollThumb" TargetType="{x:Type Thumb}">
        <Setter Property="Tag" Value="Vertical"/>
        <Setter Property="Background" Value="Gray"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <!-- 设置圆角矩形背景，圆角半径为 3 -->
                    <Border x:Name="Rectangle" CornerRadius="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{TemplateBinding Background}"/>

                    <!-- 设置触发器 -->
                    <ControlTemplate.Triggers>
                        <!-- 当滑块为纵向时，滑块宽度为 2 -->
                        <Trigger Property="Tag" Value="Vertical">
                            <Setter TargetName="Rectangle" Property="Height" Value="Auto"/>
                            <Setter TargetName="Rectangle" Property="Width" Value="2"/>
                        </Trigger>
                        <!-- 当滑块为纵向且鼠标位于滑块上时，通过动画改变宽度 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Tag" Value="Vertical"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Rectangle" Storyboard.TargetProperty="Width" From="2" To="6" Duration="0:0:0.2" FillBehavior="HoldEnd"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Rectangle" Storyboard.TargetProperty="Width" From="6" To="2" Duration="0:0:0.2" FillBehavior="HoldEnd">
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>

                        <!-- 当滑块为横向时，滑块高度为 2 -->
                        <Trigger Property="Tag" Value="Horizontal">
                            <Setter TargetName="Rectangle" Property="Width" Value="Auto"/>
                            <Setter TargetName="Rectangle" Property="Height" Value="2"/>
                        </Trigger>
                        <!-- 当滑块为横向且鼠标位于滑块上时，通过动画改变高度 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Tag" Value="Horizontal"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Rectangle" Storyboard.TargetProperty="Height" From="2" To="6" Duration="0:0:0.2" FillBehavior="HoldEnd">
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Rectangle" Storyboard.TargetProperty="Height" From="6" To="2" Duration="0:0:0.2" FillBehavior="HoldEnd">
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 自定义滚动条右键菜单的整体样式，包含阴影和圆角 -->
    <Style x:Key="CustomScrollBarContextMenuStyle" TargetType="ContextMenu">
        <!-- 去除边框色 -->
        <Setter Property="BorderBrush" Value="{x:Null}"/>
        <!-- 菜单背景色 -->
        <Setter Property="Background" Value="White"/>
        <!-- 菜单宽度 -->
        <Setter Property="Width" Value="160"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <!-- 外层Grid用于叠加阴影和内容，Margin为菜单整体留出空间 -->
                    <Grid Margin="5">
                        <!-- 阴影层，使用DropShadowEffect实现阴影 -->
                        <Border Background="White" CornerRadius="3" SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect Color="#44000000" BlurRadius="6" ShadowDepth="1" Direction="0"/>
                            </Border.Effect>
                        </Border>
                        <!-- 主体层，显示菜单内容，带圆角和边框 -->
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <StackPanel IsItemsHost="True" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 自定义滚动条右键菜单中每一项（MenuItem）的样式 -->
    <Style x:Key="CustomScrollBarMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Height="{TemplateBinding Height}">
                        <ContentPresenter ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="40,0" RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- 鼠标悬停或高亮时高亮背景 -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#F3F3F3"/>
                        </Trigger>
                        <!-- 禁用时降低透明度 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 自定义滚动条右键菜单中的分割线（Separator）样式 -->
    <Style x:Key="CustomSeparatorStyle" TargetType="Separator">
        <!-- 分割线水平拉伸 -->
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <!-- 上下间距 -->
        <Setter Property="Margin" Value="0,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Grid>
                        <!-- 分割线本体，1像素高，浅灰色 -->
                        <Border Height="1" Background="#FFF0F0F0" Margin="0" HorizontalAlignment="Stretch"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
        <!-- 轨道背景色设置为透明 -->
        <Setter Property="Background" Value="Transparent"/>
        <!-- 修改轨道宽度为 6 -->
        <Setter Property="Width" Value="6"/>
        <!-- 自定义滚动条右键菜单 -->
        <Setter Property="ContextMenu">
            <Setter.Value>
                <ContextMenu Style="{StaticResource CustomScrollBarContextMenuStyle}">
                    <MenuItem Header="滚动至此" Margin="0,3,0,0" Command="ScrollBar.ScrollHereCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                    <Separator Style="{StaticResource CustomSeparatorStyle}"/>
                    <MenuItem Header="顶部" Command="ScrollBar.ScrollToTopCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                    <MenuItem Header="底部" Command="ScrollBar.ScrollToBottomCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                    <Separator Style="{StaticResource CustomSeparatorStyle}"/>
                    <MenuItem Header="向上翻页" Command="ScrollBar.PageUpCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                    <MenuItem Header="向下翻页" Command="ScrollBar.PageDownCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                    <Separator Style="{StaticResource CustomSeparatorStyle}"/>
                    <MenuItem Header="向上滚动" Command="ScrollBar.LineUpCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                    <MenuItem Header="向下滚动" Margin="0,0,0,3" Command="ScrollBar.LineDownCommand" Style="{StaticResource CustomScrollBarMenuItemStyle}"/>
                </ContextMenu>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <!-- 滚动条的整体布局容器 -->
                    <Grid x:Name="GridRoot" Width="6" Height="Auto" Background="Transparent">
                        <!-- 定义滚动条的轨道及其子元素 -->
                        <Track x:Name="PART_Track" Grid.Row="0" IsDirectionReversed="True" Focusable="False">
                            <!-- 使用自定义的 ScrollThumbs 样式 -->
                            <Track.Thumb>
                                <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" Style="{DynamicResource ScrollThumb}"/>
                            </Track.Thumb>
                            <!-- 隐藏向上箭头按钮 -->
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Visibility="Collapsed"/>
                            </Track.IncreaseRepeatButton>
                            <!-- 隐藏向下箭头按钮 -->
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Visibility="Collapsed"/>
                            </Track.DecreaseRepeatButton>
                        </Track>
                    </Grid>

                    <!-- 设置触发器 -->
                    <ControlTemplate.Triggers>
                        <!-- 当拖拽 Thumb 时，改变背景色 -->
                        <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
                            <Setter TargetName="Thumb" Property="Background" Value="#4D4D4D"/>
                        </Trigger>
                        <!-- 当释放 Thumb 时，还原背景色 -->
                        <Trigger SourceName="Thumb" Property="IsDragging" Value="False">
                            <Setter TargetName="Thumb" Property="Background" Value="Gray"/>
                        </Trigger>
                        <!-- 当滚动条被禁用时，隐藏 Thumb -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <!-- 当滚动条方向为横向时，调整布局 -->
                        <Trigger Property="Orientation" Value="Horizontal">
                            <!-- 转换为横向滚动条布局 -->
                            <Setter TargetName="GridRoot" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="PART_Track" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90"/>
                                </Setter.Value>
                            </Setter>
                            <!-- 设置横向滚动条的宽高 -->
                            <Setter Property="Width" Value="Auto"/>
                            <!-- 修改横向滚动条的高度为 4 -->
                            <Setter Property="Height" Value="4"/>
                            <!-- 设置 Thumb 的 Tag 属性为 "Horizontal" -->
                            <Setter TargetName="Thumb" Property="Tag" Value="Horizontal"/>
                            <!-- 更改分页命令 -->
                            <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                            <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>