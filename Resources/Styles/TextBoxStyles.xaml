<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>
    <!-- 定义TextBox右键菜单 -->
    <ContextMenu x:Key="TextBoxCustomContextMenu" Style="{StaticResource CustomContextMenuStyle}">
        <MenuItem Header="剪切" Command="ApplicationCommands.Cut" Style="{StaticResource CustomMenuItemStyle}"/>
        <MenuItem Header="复制" Command="ApplicationCommands.Copy" Style="{StaticResource CustomMenuItemStyle}"/>
        <MenuItem Header="粘贴" Command="ApplicationCommands.Paste" Style="{StaticResource CustomMenuItemStyle}"/>
        <Separator Style="{StaticResource CustomSeparatorStyle}"/>
        <MenuItem Header="选择所有" Command="ApplicationCommands.SelectAll" Style="{StaticResource CustomMenuItemStyle}"/>
    </ContextMenu>
    <Style TargetType="TextBox">
        <Setter Property="ContextMenu" Value="{StaticResource TextBoxCustomContextMenu}"/>
    </Style>

    <!-- 添加动作窗口地址文本框 -->
    <Style x:Key="AddressTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="FontFamily" Value="{DynamicResource GlobalFontFamily}"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="MinHeight" Value="25"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border x:Name="border" CornerRadius="3,0,0,3" ContextMenu="{DynamicResource TextBoxCustomContextMenu}" Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="#FFD5D5D5">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="2,0" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FFABADB3"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FF326CF3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- 圆角文本框 -->
    <Style x:Key="RoundedTextBox" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{DynamicResource GlobalFontFamily}"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border x:Name="border" CornerRadius="3" BorderThickness="1" BorderBrush="#FFD5D5D5">
                        <TextBox Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="{x:Null}" VerticalAlignment="Center" Margin="2,0" Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FFABADB3"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FF326CF3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>