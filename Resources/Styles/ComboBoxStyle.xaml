<ResourceDictionary 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:Quicker.Resources.Styles"
    x:Class="Quicker.Resources.Styles.ComboBoxStyle"
>
    <!-- 内容克隆转换器资源 -->
    <local:ContentCloneConverter x:Key="ContentCloneConverter"/>
    
    <!-- 自定义 ComboBoxItem 样式 -->
    <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Height" Value="20"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="border">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E6E6E6"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#D9D9D9"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 圆角 ComboBox 的样式 -->
    <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
        <Setter Property="Height" Value="20"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" 
                                     ClickMode="Press" 
                                     Focusable="false"
                                     IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                     IsTabStop="False">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid>
                                        <Border x:Name="border" Background="#FAFAFA" BorderBrush="#FFABADB3" BorderThickness="1" CornerRadius="4">
                                            <ContentControl
                                                x:Name="contentPresenter"
                                                Margin="10,0,20,0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                HorizontalContentAlignment="Left"
                                                VerticalContentAlignment="Center"
                                                Content="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}, Converter={StaticResource ContentCloneConverter}}" />
                                        </Border>
                                        <Path
                                            x:Name="arrow"
                                            Data="M0,0 L4,4 L8,0"
                                            Stroke="#FFABADB3"
                                            Width="10"
                                            Height="6"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            Margin="0,0,10,0"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                             <!--改变边框颜色--> 
                                            <Setter Property="BorderBrush" TargetName="border" Value="#FF5B5B5B"/>
                                             <!--改变箭头颜色--> 
                                            <Setter Property="Stroke" TargetName="arrow" Value="#FF5B5B5B"/>
                                        </Trigger>
                                        
                                        <Trigger Property="IsChecked" Value="True">
                                             <!--改变边框颜色--> 
                                            <Setter Property="BorderBrush" TargetName="border" Value="#FF326CF3"/>
                                             <!--改变箭头颜色--> 
                                            <Setter Property="Stroke" TargetName="arrow" Value="#FF326CF3"/>
                                             <!--改变箭头方向--> 
                                            <Setter Property="Data" TargetName="arrow" Value="M0,4 L4,0 L8,4"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding Path=HasItems, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="false">
                                            <Setter Property="Visibility" TargetName="arrow" Value="Collapsed"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="true"
                               Focusable="false"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               PopupAnimation="Slide"
                               Width="{Binding ActualWidth, ElementName=ToggleButton}"
                               Placement="Bottom">
                            <Border
                                x:Name="dropDownBorder"
                                Background="White"
                                CornerRadius="4"
                                Margin="2">
                                <Border.Effect>
                                    <DropShadowEffect Color="#FFBBBBBB" Direction="0" BlurRadius="10" RenderingBias="Quality" ShadowDepth="1"/>
                                </Border.Effect>
                                <ScrollViewer x:Name="DropDownScrollViewer" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="true">
                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="false">
                            <Setter Property="Height" TargetName="dropDownBorder" Value="95"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter Property="Background" Value="#FFF4F4F4"/>
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="true">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <EventSetter Event="SelectionChanged" Handler="ComboBox_SelectionChanged"/>
        <EventSetter Event="Loaded" Handler="ComboBox_Loaded"/>
    </Style>
</ResourceDictionary>