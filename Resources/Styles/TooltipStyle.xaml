<ResourceDictionary 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
>
    <!-- 圆角气泡 -->
    <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}">
        <Setter Property="FontFamily" Value="{DynamicResource GlobalFontFamily}"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolTip}">
                    <Grid>
                        <!-- 阴影效果 -->
                        <Border CornerRadius="3" Background="White" Margin="3">
                            <Border.Effect>
                                <DropShadowEffect Color="LightGray" Direction="0" BlurRadius="6" RenderingBias="Quality" ShadowDepth="1"/>
                            </Border.Effect>
                        </Border>
                        <!-- 内容区域 -->
                        <Border Background="White" BorderBrush="#C5CFD8" BorderThickness="1" CornerRadius="3" Margin="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>