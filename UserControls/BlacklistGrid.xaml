<UserControl
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Quicker.UserControls"
    x:Class="Quicker.UserControls.BlacklistGrid"
    mc:Ignorable="d"
    
    Background="White"
    d:DesignHeight="870"
    d:DesignWidth="982"
>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="952*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="BlacklistButton"  Content="黑名单应用" ToolTip="设定需要禁用Quicker的软件" Style="{StaticResource Button3}" Margin="1,13,0,0" Width="91" Grid.Column="1" Height="30"/>
        <Grid x:Name="BlacklistButtonGrid" Margin="10,43,0,0" Visibility="Visible" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="643*"/>
                <ColumnDefinition Width="329*"/>
            </Grid.ColumnDefinitions>
            <Label Content="禁用Quicker的应用程序" HorizontalAlignment="Left" Margin="0,16,0,0" VerticalAlignment="Top" FontWeight="Bold" FontSize="16" Height="30" Width="184"/>
            <Border BorderBrush="#FFD5D5D5" BorderThickness="1" Margin="10,52,3,565" CornerRadius="3" MouseEnter="ShowBlacklistScrollBar" MouseLeave="HideBlacklistScrollBar">
                <Grid>
                    <ScrollViewer x:Name="BlacklistStackScrollViewer" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" ScrollChanged="BlacklistStackScrollViewer_ScrollChanged">
                        <StackPanel x:Name="BlacklistStackPanel" Margin="0,0,0,2"/>
                    </ScrollViewer>
                    <ScrollBar x:Name="BlacklistScrollBar" Margin="604,0,0,0" ValueChanged="BlacklistScrollBar_ValueChanged" HorizontalAlignment="Left" VerticalAlignment="Center" Height="171"/>
                </Grid>
            </Border>
            <Button x:Name="BlacklistDragButton" Content="拖动到要禁用Quicker的窗口上" Style="{StaticResource WhiteButton}" ToolTip="点击或按住拖动。点击进入选择模式时可最小化当前窗口。" HorizontalAlignment="Left" Margin="10,230,0,0" VerticalAlignment="Top" Width="204" Height="27" Click="BlacklistDragButton_Click"/>
            <Button x:Name="BlacklistAddButton" Content="添加..." Style="{StaticResource WhiteButton}" ToolTip="增加特殊的应用" HorizontalAlignment="Left" Margin="228,230,0,0" VerticalAlignment="Top" Width="57" Height="27" Click="BlacklistAddButton_Click"/>
            <Popup x:Name="ExistAppsPop" PopupAnimation="Fade" Width="250" Height="4" PlacementTarget="{Binding ElementName=BlacklistAddButton}" Placement="Bottom" AllowsTransparency="True" StaysOpen="False">
                <Grid>
                    <Rectangle Fill="White" Margin="5,7">
                        <Rectangle.Effect>
                            <DropShadowEffect Color="#FFBBBBBB" Direction="0" BlurRadius="15" RenderingBias="Quality" ShadowDepth="1"/>
                        </Rectangle.Effect>
                    </Rectangle>
                    <Border Width="240" Background="White" BorderThickness="0" CornerRadius="3" Margin="5">
                        <StackPanel x:Name="ExistAppsStackPanel" Margin="-1.6,2"/>
                    </Border>
                </Grid>
            </Popup>
            <Button x:Name="UnknownProcessButton" Content="无法识别的应用" Style="{StaticResource WhiteButton}" ToolTip="无法识别的应用统一使用unknown-proc.exe表示。" HorizontalAlignment="Left" Margin="295,230,0,0" VerticalAlignment="Top" Width="96" Height="27" Click="UnknownProcessButton_Click"/>
            <Button x:Name="AddDirectoryButton" Content="添加目录" Style="{StaticResource WhiteButton}" ToolTip="目录中所有程序都加入黑名单" HorizontalAlignment="Left" Margin="402,230,0,0" VerticalAlignment="Top" Width="62" Height="27" Click="AddDirectoryButton_Click"/>
            <Label Content="全屏状态禁用Quicker" HorizontalAlignment="Left" Margin="0,303,0,0" VerticalAlignment="Top" FontWeight="Bold" FontSize="16" Height="30" Width="168"/>
            <CheckBox x:Name="FullScreenDisableCheckBox" Content="开启全屏或最大化禁用功能" ToolTip="通常在游戏时禁用Quicker" HorizontalAlignment="Left" Margin="10,338,0,0" VerticalAlignment="Top" Height="15" Width="174" Click="CheckBox_Click"/>
            <Label Content="这些进程即使全屏或最大化也启用Quicker" HorizontalAlignment="Left" Margin="4,358,0,0" VerticalAlignment="Top" Height="25" Width="252"/>
            <Grid Margin="0,383,0,339">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="537*"/>
                    <ColumnDefinition Width="106"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="BlacklistSelectButton" Content="选择窗口" Style="{StaticResource WhiteButton}" Width="107" Height="24" HorizontalContentAlignment="Stretch" VerticalAlignment="Top" Grid.Column="1" Click="BlacklistSelectButton_Click"/>
                <TextBox x:Name="BlacklistProcessTextBox" Style="{StaticResource RoundedTextBox}" TextWrapping="Wrap" VerticalAlignment="Top" Height="24" HorizontalContentAlignment="Stretch" Margin="10,0,1,0"/>
            </Grid>
            <Label Content="全屏时仍然启用Quicker的进程(分号分隔多个)。进程名等于exe文件名除去扩展名的部分。" Margin="4,407,0,334" Foreground="#FFD5D5D5" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" FontSize="11"/>
            <!--<Label Content="选项" HorizontalAlignment="Left" Margin="0,458,0,0" VerticalAlignment="Top" FontSize="16" FontWeight="Bold" Height="30" Width="42"/>
            <CheckBox x:Name="ApplyBlacklistToExpandHotkeysCheckBox" Content="将黑名单与全屏禁用设置应用于'扩展热键'功能" HorizontalAlignment="Left" Margin="10,493,0,0" VerticalAlignment="Top" Height="15" Width="266" Click="CheckBox_Click"/>-->
        </Grid>
    </Grid>
</UserControl>