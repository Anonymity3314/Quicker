<UserControl
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:Quicker.UserControls.SettingWindow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    x:Class="Quicker.UserControls.SettingWindow.ColorPicker"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
   
    Background="Transparent"
    Height="280"
    Width="245"
>
    <Grid>
        <Border Margin="8,10,0,0" BorderBrush="LightGray" BorderThickness="1" VerticalAlignment="Top" Height="100" HorizontalAlignment="Left" Width="202">
            <Canvas x:Name="ColorCanvas" MouseMove="ColorCanvas_MouseMove" MouseUp="ColorCanvas_MouseUp" MouseDown="ColorCanvas_MouseDown">
                <!-- 水平渐变：从白色到红色 -->
                <Rectangle Width="{Binding ActualWidth, ElementName=ColorCanvas}" Height="{Binding ActualHeight, ElementName=ColorCanvas}">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="White" Offset="0"/>
                            <GradientStop Color="Red" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <!-- 垂直渐变：从透明到黑色 -->
                <Rectangle Width="{Binding ActualWidth, ElementName=ColorCanvas}" Height="{Binding ActualHeight, ElementName=ColorCanvas}" Panel.ZIndex="1">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="Transparent" Offset="0"/>
                            <GradientStop Color="Black" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- 可拖动的圆形 -->
                <Border x:Name="ColorThumb" Width="10" Height="10" Canvas.Left="0" Canvas.Top="0" Background="Transparent" BorderBrush="Black" BorderThickness="0.8" CornerRadius="5" Panel.ZIndex="2">
                    <Border Background="Transparent" BorderBrush="White" BorderThickness="2.5" CornerRadius="3">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.5"/>
                        </Border.Effect>
                    </Border>
                </Border>
            </Canvas>
        </Border>

        <!-- 颜色选择器滑块 -->
        <Slider x:Name="HueSlider" Minimum="0" Maximum="360" Style="{StaticResource ColorPickerSlider}" VerticalAlignment="Top" Height="130" ValueChanged="HueSlider_ValueChanged" HorizontalAlignment="Left" Margin="215,140,0,0">
            <Slider.RenderTransform>
                <ScaleTransform ScaleY="-1"/>
            </Slider.RenderTransform>
            <Slider.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Offset="0" Color="#FF0000"/>
                    <GradientStop Offset="0.1667" Color="#FF00FF"/>
                    <GradientStop Offset="0.333" Color="#0000FF"/>
                    <GradientStop Offset="0.500" Color="#00FFFF"/>
                    <GradientStop Offset="0.667" Color="#00FF00"/>
                    <GradientStop Offset="0.83" Color="#FFFF00"/>
                    <GradientStop Offset="1.00" Color="#FF0000"/>
                </LinearGradientBrush>
            </Slider.Background>
        </Slider>

        <!-- 颜色预览 -->
        <Border Height="25" BorderBrush="LightGray" BorderThickness="1" Margin="10,115,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="109">
            <Rectangle x:Name="ColorPreview">
                <Rectangle.Fill>
                    <SolidColorBrush Color="{Binding SelectedColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Rectangle.Fill>
            </Rectangle>
        </Border>

        <!-- 十六进制颜色值 -->
        <TextBox x:Name="HexValue" Style="{StaticResource RoundedTextBox}" LostFocus="HexValue_LostFocus" Margin="124,115,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="86"/>

        <StackPanel Margin="10,145,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="221">
            <!-- R 值滑块 -->
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="144"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="R" VerticalAlignment="Center" Height="16"/>
                <Slider x:Name="RedSlider" Minimum="0" Maximum="255" Width="NaN" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="RedValue" Text="{Binding ElementName=RedSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
            <!-- G 值滑块 -->
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="144"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="G" VerticalAlignment="Center"/>
                <Slider x:Name="GreenSlider" Minimum="0" Maximum="255" Width="NaN" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="GreenValue" Text="{Binding ElementName=GreenSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
            <!-- B 值滑块 -->
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="144"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="B" VerticalAlignment="Center"/>
                <Slider x:Name="BlueSlider" Minimum="0" Maximum="255" Width="NaN" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="BlueValue" Text="{Binding ElementName=BlueSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
            <!-- A 值滑块 -->
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="144"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="A" VerticalAlignment="Center"/>
                <Slider x:Name="AlphaSlider" Minimum="0" Maximum="255" Width="NaN" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="AlphaValue" Text="{Binding ElementName=AlphaSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>