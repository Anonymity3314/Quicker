<UserControl
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Quicker.UserControls"
    x:Class="Quicker.UserControls.ColorPicker"
    mc:Ignorable="d"
   
    Background="White"
    d:DesignHeight="345"
    d:DesignWidth="345"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Margin="8,10,35,10" BorderBrush="LightGray" BorderThickness="1">
            <Canvas x:Name="ColorCanvas" MouseMove="ColorCanvas_MouseMove" MouseUp="ColorCanvas_MouseUp" MouseDown="ColorCanvas_MouseDown">
                <!-- 水平渐变：从白色到红色 -->
                <Rectangle Width="{Binding ActualWidth, ElementName=ColorCanvas}" Height="{Binding ActualHeight, ElementName=ColorCanvas}">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="White" Offset="0"/>
                            <GradientStop Color="Red" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <!-- 垂直渐变：从透明到黑色 -->
                <Rectangle Width="{Binding ActualWidth, ElementName=ColorCanvas}" Height="{Binding ActualHeight, ElementName=ColorCanvas}" Panel.ZIndex="1">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="Transparent" Offset="0"/>
                            <GradientStop Color="Black" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                
                <!-- 可拖动的圆形 -->
                <Border x:Name="ColorThumb" 
                    Width="16"
                    Height="16" 
                    Canvas.Left="0" 
                    Canvas.Top="0"
                    Background="Transparent"
                    BorderBrush="White"
                    BorderThickness="3.5"
                    CornerRadius="8"/>
            </Canvas>
        </Border>

        <!-- 颜色选择器滑块 -->
        <Slider x:Name="HueSlider" Value="0" Minimum="0" Maximum="360" Style="{StaticResource ColorPickerSlider}" VerticalAlignment="Top" Margin="319,10,10,0" Height="174" ValueChanged="HueSlider_ValueChanged">
            <Slider.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="#FF0000" />
                    <GradientStop Offset="0.1667" Color="#FFFF00" />
                    <GradientStop Offset="0.333" Color="#00FF00" />
                    <GradientStop Offset="0.500" Color="#00FFFF" />
                    <GradientStop Offset="0.667" Color="#0000FF" />
                    <GradientStop Offset="0.83" Color="#FF00FF" />
                    <GradientStop Offset="1.00" Color="#FF0000" />
                </LinearGradientBrush>
            </Slider.Background>
        </Slider>

        <StackPanel Grid.Row="1" Margin="10,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="325">
            <!-- 颜色预览 -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="颜色预览:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                
                <Border Grid.Column="1" Height="25" BorderBrush="LightGray" BorderThickness="1">
                    <Rectangle x:Name="ColorPreview">
                        <Rectangle.Fill>
                            <SolidColorBrush Color="{Binding SelectedColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Rectangle.Fill>
                    </Rectangle>
                </Border>
            </Grid>
            
            <!-- 十六进制颜色值 -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="十六进制:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="HexValue" Text="#FFFFFFFF" Grid.Column="1" TextChanged="HexValue_TextChanged"/>
            </Grid>
            
            <!-- R 值滑块 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="R" VerticalAlignment="Center"/>
                <Slider x:Name="RedSlider" Minimum="0" Maximum="255" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="RedValue" Text="{Binding ElementName=RedSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Margin="55,0,-45,0" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
            <!-- G 值滑块 -->
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="G" VerticalAlignment="Center"/>
                <Slider x:Name="GreenSlider" Minimum="0" Maximum="255" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="GreenValue" Text="{Binding ElementName=GreenSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Margin="55,0,-45,0" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
            <!-- B 值滑块 -->
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="B" VerticalAlignment="Center"/>
                <Slider x:Name="BlueSlider" Minimum="0" Maximum="255" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="BlueValue" Text="{Binding ElementName=BlueSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Margin="55,0,-45,0" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
            <!-- A 值滑块 -->
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="A" VerticalAlignment="Center"/>
                <Slider x:Name="AlphaSlider" Minimum="0" Maximum="255" Grid.Column="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Slider_ValueChanged"/>
                <TextBox x:Name="AlphaValue" Text="{Binding ElementName=AlphaSlider, Path=Value, Mode=TwoWay}" Grid.Column="2" Margin="55,0,-45,0" Width="40" TextAlignment="Right" TextChanged="Value_TextChanged"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>